apply {
    from("$rootDir/compose-module.gradle")
}

android {
    namespace = app_id + ".core_ui"
}

dependencies {
    implementation project(":core")
    implementation "io.coil-kt:coil-compose:$coil_version"
}